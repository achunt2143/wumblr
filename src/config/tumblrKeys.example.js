// Copy this file to `tumblrKeys.js` (gitignored) and fill in your own values.
// Register an app at https://www.tumblr.com/oauth/apps to get a consumer
// key/secret pair. Never commit the real `tumblrKeys.js` file.

export const consumerKey = 'your-tumblr-consumer-key';
export const consumerSecret = 'your-tumblr-consumer-secret';
